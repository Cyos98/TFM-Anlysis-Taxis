# Configuración

`pipeline.toml` contiene valores no secretos y versionables. Las variables de entorno pueden sobrescribir rutas, entorno y conexión sin modificar el fichero.

El periodo demo actual es provisional y solo se usa para validar la fundación. Debe confirmarse antes de implementar la descarga en la fase 2.
